const person = {
  name: 'naiara',
  lastName: 'gabriela',
};

console.log(person.name);

function chooseColors(colors: 'Red' | 'Blue' | 'Black') {
  return colors;
}

console.log(chooseColors('Red'));
